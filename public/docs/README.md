# Activity documents

Put PDFs or images here (e.g. `brief.pdf`, `morse.png`) and commit them.
On the live site they are served under the app's base path, so in an
activity's "Embedded document" field reference them as:

    docs/brief.pdf

They then display inline on the activity. Direct links and Google Drive
share links also work in that same field.
